# Public — digits only, country code, no + or spaces (e.g. 9198XXXXXXXX)
NEXT_PUBLIC_WHATSAPP_NUMBER=

# Server-only lead sinks (leave empty in local; leads log to console)
LEAD_NOTIFY_EMAIL=
LEAD_WEBHOOK_URL=
GOOGLE_SHEET_WEBHOOK_URL=

# Feature flag — Pro AI sections
FEATURE_PRO_AI=false

# Optional public site URL (SEO / sitemap / JSON-LD)
NEXT_PUBLIC_SITE_URL=https://eternalflow.co
